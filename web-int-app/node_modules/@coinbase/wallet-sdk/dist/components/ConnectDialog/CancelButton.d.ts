import { Theme } from "./types";
declare type CancelButtonProps = {
    onClick: () => void;
    theme: Theme;
};
export declare function CancelButton({ theme, onClick }: CancelButtonProps): import("preact").JSX.Element;
export {};
