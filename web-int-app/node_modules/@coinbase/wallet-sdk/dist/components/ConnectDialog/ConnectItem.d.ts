import { Theme } from "./types";
declare type ConnectItemProps = {
    title: string;
    description: string;
    icon: string;
    selected: boolean;
    onClick(): void;
    theme: Theme;
};
export declare function ConnectItem({ title, description, icon, selected, theme, onClick, }: ConnectItemProps): import("preact").JSX.Element;
export {};
